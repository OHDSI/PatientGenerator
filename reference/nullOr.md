# Null coalescing operator

Null coalescing operator

## Usage

``` r
x %||% y
```

## Arguments

- x:

  First value (any type).

- y:

  Fallback value when `x` is NULL.

## Value

`x` if not NULL, otherwise `y`.
