# Useful to list available datasets in the testCases folder

Useful to list available datasets in the testCases folder

## Usage

``` r
getTestSets(path = NULL)
```

## Arguments

- path:

  Optional directory containing JSON test sets. If NULL, the package
  resolves a default path with testthat integration.

## Value

A list()
